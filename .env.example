# =================================================================
# Threat Track - Environment Configuration Example
# Copy this file to .env and customize for your deployment
# =================================================================

# =================================================================
# DEPLOYMENT MODE SELECTION
# =================================================================

# Choose your deployment scenario by setting COMPOSE_PROFILES:
# - "LOCAL,media" = Docker PostgreSQL + Local media container
# - "LOCAL,S3"    = Docker PostgreSQL + S3 storage (no media container)
# - "AZURE,BLOB"  = Azure PostgreSQL + Azure Blob storage (no media container)
# - "AWS,S3"      = AWS RDS + AWS S3 storage (no media container)
COMPOSE_PROFILES=LOCAL,media

# =================================================================
# APPLICATION SETTINGS
# =================================================================
DEBUG=False
DJANGO_SECRET_KEY=your-secret-key-here-change-this-in-production # Optional
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,app,your-domain.com

# =================================================================
# DATABASE CONFIGURATION
# =================================================================

# Option 1: Docker PostgreSQL
DATABASE_TYPE=LOCAL
DB_PASSWORD=postgres

# Option 2: Azure Database for PostgreSQL
# DATABASE_TYPE=AZURE
# DATABASE_CONFIGURATION=postgresql://username@server:password@server.postgres.database.azure.com:5432/threat_track?sslmode=require

# Option 3: AWS RDS PostgreSQL  
# DATABASE_TYPE=AWS
# DATABASE_CONFIGURATION=postgresql://username:password@rds-instance.region.rds.amazonaws.com:5432/threat_track?sslmode=require

# Option 4: DigitalOcean Managed Database
# DATABASE_TYPE=AWS

# Without Certificate
# DATABASE_CONFIGURATION=Connection string (postgresql://username:password@db-postgresql-region-xxxxx-do-user-xxxxxxx-0.x.db.ondigitalocean.com:25060/defaultdb?sslmode=require)

# With Certificate (Download the CA certificate from DigitalOcean and move it to the certificates folder)
# DATABASE_CONFIGURATION=Connection string (postgresql://username:password@db-postgresql-region-xxxxx-do-user-xxxxxxx-0.x.db.ondigitalocean.com:25060/defaultdb?sslmode=verify-ca&sslrootcert=/app/certificates/ca-certificate.crt)

# =================================================================
# MEDIA STORAGE CONFIGURATION
# =================================================================
# Configure Custom Host/IP and port
# MEDIA_HOST=localhost
# MEDIA_PORT=8080

# Option 1: Local storage with Docker
MEDIA_STORAGE_TYPE=LOCAL

# Option 2: Azure Blob Storage
# MEDIA_STORAGE_TYPE=BLOB
# BLOB_CONFIGURATION=storage_account_name,storage_account_key,container_name

# Option 3: DigitalOcean Spaces or AWS S3
# MEDIA_STORAGE_TYPE=S3
# S3_CONFIGURATION=YOUR_ACCESS_KEY,YOUR_SECRET_KEY,YOUR_BUCKET_NAME,YOUR_ENDPOINT_URL,YOUR_REGION

# Examples:
# For DigitalOcean Spaces:
# S3_CONFIGURATION=DO801EXAMPLE123,your_secret_key_here,your-bucket,https://fra1.digitaloceanspaces.com,fra1